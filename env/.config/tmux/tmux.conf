# Theme
source-file ~/.dotfiles/env/.config/tmux/sarnai.conf

# Leader
set -g prefix C-s

# Reload config <leader>+r
unbind r
bind r source-file ~/.dotfiles/env/.config/tmux/tmux.conf

# True color settings
set -g default-terminal "tmux-256color"
set -as terminal-features ",xterm*:RGB"

# Mouse
set -g mouse on

# motion.nvim
bind-key -n M-h previous-window
bind-key -n M-j switch-client -n
bind-key -n M-k switch-client -p
bind-key -n M-l next-window
bind-key -n M-1 selectw -t 1
bind-key -n M-2 selectw -t 2
bind-key -n M-3 selectw -t 3
bind-key -n M-4 selectw -t 4
bind-key -n M-5 selectw -t 5
bind-key -n M-6 selectw -t 6
bind-key -n M-7 selectw -t 7
bind-key -n M-8 selectw -t 8
bind-key -n M-9 selectw -t 9

# sessions by alphabetical order
bind s choose-tree -s -O name

# clipboard
set -s set-clipboard on
setw -g mode-keys vi
unbind -T copy-mode-vi MouseDragEnd1Pane
bind -T copy-mode-vi y send-keys -X copy-pipe

# Set index to 1 instead of 0
set -g base-index 1
setw -g pane-base-index 1

# Restore clear on Ctrl+l
unbind-key -n C-l
