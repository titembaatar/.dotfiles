focus:
  - 6
  - 1

workspaces:
  # 2:
  #   layout: h
  #   containers:
  #     - app: davinciresolve
  #       cmd: "/opt/resolve/bin/resolve"
  #       delay: 5
  6:
    layout: v
    containers:
      - app: plexamp
        cmd: "/usr/bin/flatpak run --branch=stable --arch=x86_64 --command=startplexamp com.plexamp.Plexamp"
        size: 25
        delay: 3
      - app: firefox
        delay: 1
        post:
          - "firefox --new-tab home.titem.top"
  7:
    layout: v
    containers:
      - app: discord
        cmd: "gtk-launch discord"
        size: 60
        delay: 3
      - app: firefox
        size: 40
        delay: 1
      - app: kitty
        cmd: "kitty -d /mnt/yesugei/souen/mega/"
        size: 10
  8:
    layout: v
    containers:
      - app: firefox
        size: 33
        delay: 1
        post:
        - "firefox --new-tab home.titem.top"
        - "firefox --new-tab uptime.titem.top"
        - "firefox --new-tab beszel.titem.top"
  # 10:
  #   layout: h
  #   containers:
  #     - app: mega
  #       cmd: "megasync"
        # post:
        #   - "wtype -k Escape"

# workspaces:
#   1:
#     layout: t
#     containers:
#       - app: kitty
#         post:
#           - "wtype tmux -k Return"
#           - "wtype -M ctrl s -M ctrl r"
#       - app: zen
#         cmd: "zen-browser --browser"
#         post:
#           - "wtype -M ctrl -M alt 2"
#           - "wtype -M alt 4"
