#!/usr/bin/env bash

source $HOME/.config/fzf/config

music_path="/mnt/data/media/music"

selected_album=$(find $music_path -mindepth 2 -maxdepth 2 -type d | \
  sed "s|$music_path/||" | \
  fzf --prompt "music: ")

cmus-remote -c -q "$music_path/$selected_album"
sleep 0.1
cmus-remote -n -p
