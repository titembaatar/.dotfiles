#!/usr/bin/env bash
set -e
source $HOME/.config/fzf/config

music_path="/mnt/juerbiesu/media/music"

selected_album=$(find $music_path -mindepth 2 -maxdepth 2 -type d | \
  sed "s|$music_path/||" | \
  fzf --prompt "music: ")

cmus-remote -q "$music_path/$selected_album"
cmus-remote -n
cmus-remote -p
