# Leader
set -g prefix C-s

# Reload config <leader>+r
unbind r
bind r source-file ~/.dotfiles/tmux/.config/tmux/tmux.conf

# True color settings
set -g default-terminal "$TERM"
set -ag terminal-overrides ",$TERM:Tc"

# Mouse on
set -g mouse on

# motion.nvim
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# clipboard
set -s set-clipboard on
setw -g mode-keys vi
unbind -T copy-mode-vi MouseDragEnd1Pane
bind -T copy-mode-vi y send-keys -X copy-pipe

# Set index to 1 instead of 0
set -g base-index 1
setw -g pane-base-index 1

# TPM
set-environment -g TMUX_PLUGIN_MANAGER_PATH '~/.dotfiles/tmux/.config/tmux/plugins/'

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'christoomey/vim-tmux-navigator'

run '~/.dotfiles/tmux/.config/tmux/plugins/tpm/tpm'

# tmux-resrrect
set -g @resurrect-hook-post-restore-all '~/.dotfiles/tmux/.config/tmux/scripts/resurrect/hooks/post-restore-all.sh'

### Theme
  # Sarnai Colors
	set -ogq @base				"#172620"
	set -ogq @surface			"#21362d"
	set -ogq @overlay			"#2d493d"
	set -ogq @muted				"#4e7e6b"
	set -ogq @subtle			"#90bbaa"
	set -ogq @text				"#dfece7"
	set -ogq @sarnai			"#f0c3cb"
	set -ogq @anis				"#ff6b6b"
	set -ogq @chatsalgan	"#e5951a"
	set -ogq @els					"#cca24d"
	set -ogq @uvs					"#80b946"
	set -ogq @nuur				"#2b879e"
	set -ogq @mus					"#9deaea"
	set -ogq @yargui			"#d5b3e5"
	# Messages
	set -g message-style "fg=#{@base} bg=#{@chatsalgan}"
	set -g message-command-style "fg=#{@base} bg=#{@els}"
	# Panes
	set -g pane-border-style "fg=#{@muted}"
	set -g pane-active-border-style "fg=#{@sarnai}"
	# Status global
	set -g status-position top
	set -g status-keys vi
	set -g status-style "fg=#{@text} bg=#{@base}"
	# Status left
	set -g status-left " #[fg=#{@base} bg=#{@mus}] [#S] #[fg=#{@base} bg=#{@base}] "
	# Window active
	set -g window-status-current-format " #I #W#F "
	set -g window-status-current-style "fg=#{@base} bg=#{@sarnai}"
	# Window inactive
	set -g window-status-format " #I #W#F "
	set -g window-status-style "fg=#{@muted} bg=#{@surface}"
	# Status right
	set -g status-right "#[align=absolute-centre] #T #[align=right]"
	set -g status-right-style "fg=#{@subtle}"

